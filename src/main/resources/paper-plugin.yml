# noinspection YAMLSchemaValidation
name: ${baseCoordinates}
main: eu.xap3y.gungame.GunGame
version: ${version}
api-version: '1.20'
description: GunGame minigame
has-open-classloader: false

author: XAP3Y
website: https://pvpcraft.cz

load: POSTWORLD

dependencies:
    server:
        Vault:
            required: true
        XaLib:
            required: true
            load: BEFORE
        PlaceholderAPI:
            required: false
        Multiverse-Core:
            required: false
            load: BEFORE
        LiteEco:
            required: false